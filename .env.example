# .env.example
# Copy this file to .env.local and fill in your actual values.
# Do not commit .env.local to version control.

# --- Resend (Email Sending) --- #
RESEND_API_KEY=                           # REQUIRED: Your secret API key from Resend (resend.com). Used server-side.
EMAIL_FROM_ADDRESS=                       # REQUIRED: Email address to send emails FROM (e.g., "Your Name <you@yourverifieddomain.com>"). Must be linked to a domain verified in Resend.
CONTACT_EMAIL=                            # REQUIRED: Email address to send contact form submissions TO.

# --- Cloudflare Turnstile (Bot Protection) --- #
NEXT_PUBLIC_TURNSTILE_SITE_KEY=           # REQUIRED: Your public Site Key from Cloudflare Turnstile dashboard. Used client-side.
TURNSTILE_SECRET_KEY=                   # REQUIRED: Your secret key from Cloudflare Turnstile dashboard. Used server-side.

# --- Google Maps --- #
NEXT_PUBLIC_GOOGLE_MAPS_EMBED_URL=        # Required: The embed URL obtained from Google Maps "Share or embed map". Needed for contact page map.

# --- Calendly Integration (Optional) --- #
CALENDLY_API_KEY=                         # OPTIONAL: Required only if using Calendly API features.
NEXT_PUBLIC_CALENDLY_USERNAME=            # OPTIONAL: Your Calendly username for embedding the widget.

# --- General --- #
NEXT_PUBLIC_BASE_URL=http://localhost:3000 # OPTIONAL: Base URL of the deployed site (e.g., https://yourdomain.com). Used for absolute URLs in emails, meta tags etc. Default is localhost for development.

# --- Google Analytics --- #
NEXT_PUBLIC_GA_MEASUREMENT_ID=         # OPTIONAL: Your Google Analytics 4 Measurement ID (e.g., G-XXXXXXXXXX) for tracking website traffic.

# --- Sanity CMS --- #
NEXT_PUBLIC_SANITY_PROJECT_ID=        # REQUIRED: Your Sanity project ID (get from sanity.io/manage)
NEXT_PUBLIC_SANITY_DATASET=production # REQUIRED: Dataset name (usually 'production')
SANITY_API_TOKEN=                     # REQUIRED: API token with write permissions (for migration and Studio)
